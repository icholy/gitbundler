services:
  gitbundler:
    build: .
    ports:
      - "8080:8080"
    working_dir: /app
    volumes:
      - ./gitbundler.yaml:/app/gitbundler.yaml:ro
      - gitbundler-data:/app/data

volumes:
  gitbundler-data:
